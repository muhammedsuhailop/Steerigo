import { IEventBus } from "../../application/services/IEventBus";
import { IRideNotificationService } from "../../application/services/IRideNotificationService";
import { INotificationPersistenceService } from "../../application/services/NotificationPersistenceService";
import { RideDomainEvent } from "../../application/events/RideEvents";
import { PaymentDomainEvent } from "../../application/events/PaymentEvents";
import { IPaymentNotificationService } from "../../application/services/IPaymentNotificationService";
import { IDriverAvailabilityRepository } from "../../domain/repositories/IDriverAvailabilityRepository";
import { IDriverRepository } from "../../domain/repositories/IDriverRepository";
import { DomainEvent } from "../../application/events/DomainEvent";
import { IEventHandler } from "../../application/events/IEventHandler";
import { FutureRideDomainEvent } from "../../application/events/FutureRideEvents";
import { IChatRoomExpiryService } from "../../application/services/chat/IChatRoomExpiryService";
import { IChatRoomRepository } from "../../domain/repositories/IChatRoomRepository";
export declare class InMemoryEventBus implements IEventBus {
    private readonly notificationService;
    private readonly persistence;
    private readonly paymentNotificationService;
    private readonly driverRepository;
    private readonly driverAvailabilityRepository;
    private readonly chatRoomExpiryService;
    private readonly chatRoomRepository;
    private readonly handlers;
    constructor(notificationService: IRideNotificationService, persistence: INotificationPersistenceService, paymentNotificationService: IPaymentNotificationService, driverRepository: IDriverRepository, driverAvailabilityRepository: IDriverAvailabilityRepository, chatRoomExpiryService: IChatRoomExpiryService, chatRoomRepository: IChatRoomRepository);
    subscribe<TEvent extends DomainEvent>(eventType: TEvent["type"], handler: IEventHandler<TEvent>): void;
    publish(event: RideDomainEvent | PaymentDomainEvent | FutureRideDomainEvent): Promise<void>;
    private dispatchRegisteredHandlers;
    private handleRideRequestCreated;
    private handleRideMatched;
    private handleRideArrived;
    private handleRideStarted;
    private handleRideCompleted;
    private handlePaymentInitiated;
    private handlePaymentSucceeded;
    private handlePaymentFailed;
    private handlePaymentCashConfirmed;
    private handleRideCancelled;
    private handleRideCancelledByDriver;
    private handleRideFareUpdated;
    private handleRideSearchProgressUpdated;
    private updateDriverAvailability;
    private incrementDriverRideCount;
    private handleFutureRideRequestSentToDriver;
    private handleFutureRideAccepted;
    private handleFutureRideExpired;
    private handleFutureRideCancelledByRider;
    private handleFutureRideRequestExpiredForDriver;
    private handleFutureRideRequestCancelledForDriver;
    private handleRideRequestExpiredForDriver;
    private handleFutureRideLastRequestRejected;
    private scheduleChatRoomEndForRide;
    private scheduleChatRoomEndAfterCancellation;
}
//# sourceMappingURL=InMemoryEventBus.d.ts.map